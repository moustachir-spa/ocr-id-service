# Contributing

Thanks for helping improve OCR ID Service. Run `uv sync`, `uv run pytest`, `uv run ruff check .`, and `uv run mypy app scripts` before opening a pull request.

Do not add real identity documents, personal data, credentials, or generated OCR output. Use synthetic or legally redistributable fixtures only. Document new format support and its known limitations in tests and the README.
